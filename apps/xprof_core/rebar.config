{deps,
 [{hdr_histogram, "~> 0.3"}
 ]}.

{erl_opts, [debug_info,
            {platform_define, "^(R|1|2(0|1))", no_atomics},
            {platform_define, "^[^R1]", ceil_floor} %% from OTP 20
           ]}.
